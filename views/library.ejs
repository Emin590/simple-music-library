<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Your Library</title>
  <link href="./output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center py-10">

  <h1 class="text-3xl font-bold mb-6 text-gray-800">Your Library</h1>

  <div class="bg-white p-6 rounded-lg shadow-md w-full max-w-md space-y-4">
    <% if (songs.length === 0) { %>
      <p class="text-gray-600 text-center">No songs added yet.</p>
    <% } else { %>
      <% songs.forEach((song) => { %>
        <div class="flex justify-between items-center p-2 border-b border-gray-200 last:border-b-0">
          <span class="font-medium text-gray-800"><%= song.title %></span>
          <span class="text-gray-600">- <%= song.artist %></span>
        </div>
      <% }) %>
    <% } %>
  </div>

  <div class="flex space-x-4 mt-6">
    <a href="/add">
      <button class="bg-indigo-500 text-white py-2 px-4 rounded hover:bg-indigo-600 transition-colors">
        Add Song
      </button>
    </a>
    <a href="/logout">
      <button class="bg-gray-500 text-white py-2 px-4 rounded hover:bg-gray-600 transition-colors">
        Log Out
      </button>
    </a>
  </div>

</body>
</html>
